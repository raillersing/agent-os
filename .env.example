# Local Agent OS development configuration. Keep the real .env uncommitted.
POSTGRES_DB=agent_os
POSTGRES_USER=agent_os
POSTGRES_PASSWORD=replace-with-a-local-password
POSTGRES_HOST_PORT=5435
REDIS_HOST_PORT=6381
BACKEND_HOST_PORT=8080
FRONTEND_HOST_PORT=3080
SECRET_KEY=replace-with-at-least-32-random-characters
ADMIN_EMAIL=admin@example.local
ADMIN_PASSWORD=replace-with-a-local-admin-password
TEMPORAL_NAMESPACE=default
TEMPORAL_TASK_QUEUE=agent-os-d0
CORS_ORIGINS=["http://localhost:3080","http://127.0.0.1:3080"]
NEXT_PUBLIC_API_URL=http://localhost:8080
